{
  "name": "perps-games",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*", "server"],
  "comment:overrides": "Pin @solana/kit to one 5.x copy so branded Address/Transaction types stay compatible across the server signing helpers.",
  "overrides": {
    "@solana/kit": "5.5.1"
  }
}
