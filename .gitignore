# Brainstorming / visual companion working files
.superpowers/
.worktrees/

# Node
node_modules/
dist/
.next/
.env
.env.local

# Release APKs are staged locally for Railway uploads, never committed.
redline3d/public/downloads/*.apk

# Spike build output (sources live on their own branches; e.g. vrf-signer-probe on onboarding-road)
spikes/**/target/
spikes/**/.anchor/
