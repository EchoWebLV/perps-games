# The Solana mobile wallet-adapter chain transitively pulls react-native, whose
# peer range (@types/react ^19) conflicts with our React 18 pin. Resolve installs
# the pre-npm-7 way (peers are warnings, not errors) — required for fresh installs
# locally AND on Vercel builds.
legacy-peer-deps=true
